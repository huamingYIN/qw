# UI 设计指南

> **设计类型**: App 设计（应用架构设计）
> **确认检查**: 本指南适用于可交互的应用/网站/工具。

> ℹ️ Section 1 为设计意图与决策上下文。Code agent 实现时以 Section 2 及之后的具体参数为准。

## 1. Design Archetype (设计原型)

### 1.1 内容理解

- **目标用户**: 办公人员/学生/设计师，需快速将图片合并为PDF，重视隐私与无损质量
- **核心目的**: 引导行动（上传→转换→下载），建立信任（本地处理、零上传）
- **情绪基调**: 专业安心 / 高效掌控；避免焦虑（等待卡顿）、廉价感（广告风）

### 1.2 设计方向

- **Design Style**: Soft Blocks 柔色块 — 工具类需亲和+专业感，柔和阴影区分功能区块，圆角降低操作压力
- **Application Type**: Tool（浏览器端在线工具）— 单页线性工作流，无侧边栏导航
- **Aesthetic Direction**: 洁净精密感：白底蓝调+微阴影层次，让「上传区」和「生成按钮」成为绝对视觉锚点

## 2. Color System (色彩系统)

**色彩关系**: #2563EB 品牌蓝主色 + HSL(215,20%,97%) 冷灰底 + HSL(215,25%,15%) 深墨文字
**配色设计理由**: 用户指定蓝色科技感+隐私信任感；冷灰底减少长时间操作疲劳，蓝主色聚焦关键行动
**主色推导**: #2563EB 直接用于「生成PDF」按钮、上传区激活态、进度条，绑定核心转换动作
**使用比例**: 60% 中性背景/卡片 / 30% 辅助灰（边框、次级文字）/ 10% 品牌蓝（仅CTA、激活态、进度指示）

### 2.1 主题颜色

| Token                | HSL 值                  | 说明                                     |
| -------------------- | ----------------------- | ---------------------------------------- |
| `background`         | hsl(215, 20%, 97%)      | 冷灰页面底色，降低纯白刺眼感             |
| `card`               | hsl(0, 0%, 100%)        | 卡片/面板背景，与底色形成微层次          |
| `foreground`         | hsl(215, 25%, 15%)      | 主文字，深蓝黑保证可读性                 |
| `muted-foreground`   | hsl(215, 15%, 50%)      | 次要说明、限制文案                       |
| `primary`            | hsl(217, 91%, 53%)      | #2563EB 品牌蓝，生成按钮/激活态          |
| `primary-foreground` | hsl(0, 0%, 100%)        | 主按钮文字                               |
| `accent`             | hsl(217, 91%, 95%)      | 极浅蓝，hover/focus/骨架屏/拖拽高亮背景  |
| `accent-foreground`  | hsl(217, 91%, 40%)      | accent 上的文字/图标                     |
| `border`             | hsl(215, 15%, 88%)      | 卡片边框、分割线                         |

### 2.2 导航区配色

- **基调关系**: 复用主配色系统，白色背景+底部细线分隔，不额外定义token
- **关键状态**: 语言/主题切换按钮 hover 用 `bg-accent`，激活态文字 `text-primary`
- **边界与背景**: 非透明 `bg-card`，底部 `border-b border-border` 分隔内容区

### 2.3 语义颜色

| 用途     | HSL 值                  | 衍生说明                              |
| -------- | ----------------------- | ------------------------------------- |
| 成功/完成 | hsl(152, 60%, 42%)     | 绿色系，转换完成提示、下载按钮        |
| 错误/失败 | hsl(0, 72%, 51%)       | 红色系，上传超限、转换失败            |
| 警告/提示 | hsl(38, 92%, 50%)      | 橙黄系，格式不支持提醒（大字号使用）  |

## 3. Typography (字体排版)

- **Heading**: Inter + system-ui, -apple-system, sans-serif
- **Body**: Inter + system-ui, -apple-system, sans-serif
- **字体策略**: Inter 几何感强、数字清晰，适配工具类精密感；回退系统字体栈确保多终端一致

## 4. Layout Strategy (布局策略)

- **导航意图**: 顶部极简导航条（Logo+工具名+语言+主题切换）；无侧边栏，单页线性工作流
- **页面架构**: 居中单列流式布局，上传区→列表→设置→结果垂直排列；`max-w-4xl`
- **响应式**: 移动端上传区全宽、图片列表单列；桌面端图片网格3-4列、设置面板可并排

## 5. Visual Language (视觉语言)

- **形态参数**: 圆角 `rounded-xl (0.75rem)` · 阴影 `shadow-sm` 卡片 / `shadow-md` 上传区激活态 · 间距基调 `standard`
- **识别签名**: 上传区虚线边框 `border-2 border-dashed` → 激活态实线蓝光晕；进度条圆角填充动画
- **装饰策略**: 无装饰图形，仅靠阴影层次、边框虚实变化和蓝色光晕建立视觉记忆
- **动效原则**: 拖拽进入/离开 200ms ease；卡片排序 250ms spring；进度条平滑过渡
- **可及性**: 对比度 ≥ 4.5:1；上传区拖拽态有颜色+边框双重反馈；焦点环 `ring-2 ring-primary`

## 6. Component Principles (组件原则)

- **状态完整性**: 上传区覆盖 idle/hover/drag-enter/drag-leave/disabled；生成按钮覆盖 idle/loading/success/error
- **层级清晰**: 「生成PDF」用 `bg-primary` 实心按钮；「重新转换」用 outline 变体；删除/旋转用 ghost icon 按钮
- **一致性**: 所有卡片统一 `rounded-xl shadow-sm border border-border p-5`；设置控件统一 label+control 垂直间距

## 7. Image Direction (图片与视觉资产，按需)

- **Image Role**: 无强制图片需求
- **Image Art Direction**: 无强制图片需求，优先通过上传区虚线边框光晕、进度条动画、优势卡片图标建立视觉记忆点
- **Image Prompt Keywords**: 无
- **Image Avoidance**: 避免通用科技插图、商务人物素材、抽象渐变背景图

## 8. 应避免 (Anti-patterns)

- ❌ 在工具页添加营销Banner或第三方广告位，破坏「本地处理隐私安全」的信任感
- ❌ 图片列表使用密集表格样式，应保持卡片网格+缩略图预览的工具直觉
- ❌ 暗色模式简单反色而不调整阴影/边框层次，导致卡片与背景融为一体丧失区块感